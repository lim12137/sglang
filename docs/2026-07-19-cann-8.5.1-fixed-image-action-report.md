# SGLang CANN 镜像 Action 报告

日期: 2026-07-19  
目录: `M:\AI\1work\sglang`  
远程: https://github.com/lim12137/sglang-cann

## 目标

1. SGLang 基础镜像：最新 CANN 8.5 上游 + 时间 tag
2. ASR 匹配镜像：时间 tag + `-asr`
3. 推送 GHCR；若配置了 `ALIYUN_ACR_PWD` 则同时推 ACR

## 首次触发结果（run 29673127850）

| Job | 结果 | 原因 |
|-----|------|------|
| resolve-tags | success | 时间 tag 生成成功 |
| build-base | failure | ACR 登录失败：仓库未配置 `ALIYUN_ACR_PWD` |
| build-asr | failure | 同上 |

GHCR 登录成功，但 ACR 密码 secret 缺失导致整体失败。

## 修复

- workflow 改为：无 ACR secret 时 **仅推 GHCR**，有 secret 时推 GHCR+ACR
- 需补 secret 才能推 ACR：

```powershell
gh secret set ALIYUN_ACR_PWD -R lim12137/sglang-cann
```

## 镜像标签

| 角色 | 时间 Tag | 滚动 Tag |
|------|----------|----------|
| 基础 | `YYYYMMDD-HHMMSS` | `latest` |
| ASR | `YYYYMMDD-HHMMSS-asr` | `latest-asr` |

GHCR: `ghcr.io/lim12137/sglang-cann:<tag>`  
ACR: `crpi-fs24haezdztsodhc.cn-guangzhou.personal.cr.aliyuncs.com/hopemyl/sglang-cann:<tag>`

## 本地测试

```text
D:\py311\python.exe -m py_compile verify/verify_npu_models.py → OK
```
