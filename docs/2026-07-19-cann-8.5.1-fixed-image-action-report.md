# SGLang CANN 镜像 Action 报告

日期: 2026-07-19  
目录: `M:\AI\1work\sglang`

## 目标

1. **SGLang 基础镜像**：每次构建用 **最新** CANN 8.5 上游（默认 `lmsysorg/sglang:main-cann8.5.0-910b`）
2. **ASR 匹配镜像**：支持运行时指定 Fun-ASR-Nano / VAD / CAM++ 模型路径
3. **按时间打 tag**：`YYYYMMDD-HHMMSS`；ASR 为 `YYYYMMDD-HHMMSS-asr`（Docker tag 不能含 `+`）
4. 推送到 GHCR + 阿里云 ACR

## 标签约定

| 角色 | 时间 Tag | 滚动 Tag |
|------|----------|----------|
| 基础 | `20260719-031522` | `latest` |
| ASR | `20260719-031522-asr` | `latest-asr` |

## 文件清单

| 文件 | 作用 |
|------|------|
| `Dockerfile` | SGLang 基础镜像 |
| `Dockerfile.asr` | ASR 匹配镜像 |
| `requirements-asr.txt` | ASR 依赖 pin |
| `verify/*` | NPU 离线校验入口 |
| `.github/workflows/build.yml` | resolve-tags + build-base + build-asr |
| `README.md` | 使用说明 |

## 测试命令与结果摘要

```text
命令: D:\py311\python.exe -m py_compile verify/verify_npu_models.py
结果: OK

命令: workflow 关键字检查（time tag / -asr / ACR / arm64）
结果: 见推送前校验输出
```

未在本机做真实 NPU 推理（无 Ascend 设备）。

## Action 触发

```bash
gh workflow run build.yml
# 或 push main/master 相关路径
```

Secret: `ALIYUN_ACR_PWD`
